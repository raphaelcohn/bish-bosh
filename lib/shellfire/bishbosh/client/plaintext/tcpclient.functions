core_usesIn core functions
core_functions_register _bishbosh_client_plaintext_registration tcpclient

# DJB's tcpclient / tcpserver (part of ucspi-tcp)
# Homebrew, Debian
bishbosh_client_plaintext_tcpclient_check()
{
	if core_compatibility_whichNoOutput tcpclient; then
		bishbosh_client_plaintext=tcpclient
		bishbosh_client_plaintextPath="$(core_compatibility_which tcpclient)"
		return 0
	fi
	return 1
}
