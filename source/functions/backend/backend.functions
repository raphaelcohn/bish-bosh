function backend_determineIfAuto()
{
	local -r backend="$1"
	local -r kind="$2"
	local -r pathsFile="$lfs_configPath"/backends/"$backend"/"$kind"/*."$distributionName".path
}

function backend_determineIfAutoClient()
{
	backend_determineIfAuto "client" "plaintext"
}